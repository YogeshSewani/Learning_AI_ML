[project]
name = "WebSummarizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yogesh", email = "yogeshsewani2004@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "ipython>=9.17.0",
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
    "streamlit",
]

[project.scripts]
ai-projects = "ai_projects:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
